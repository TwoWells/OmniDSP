# Config.cmake.in - Template for OmniDSPConfig.cmake

# Use helper macro to set up basic variables like PACKAGE_PREFIX_DIR
@PACKAGE_INIT@

# Include the file which defines the imported library targets (OmniDSP::omnidsp)
# The path is relative to the location where this OmniDSPConfig.cmake file is installed.
include("${CMAKE_CURRENT_LIST_DIR}/OmniDSPTargets.cmake")

# Optionally check for components if your library had them (OmniDSP currently doesn't)
# check_required_components(OmniDSP)